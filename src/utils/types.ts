export type T_ImageSrc = `${string}.${'png' | 'jpg' | 'jpeg'}`;

export type T_JsonSrc = `${string}.${'json'}`;

export type T_Position = { x: number; y: number };
export type T_Dimensions = { width: number; height: number };
